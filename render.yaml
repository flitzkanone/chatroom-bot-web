services:
  - type: web
    name: final-stable-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      # Wir installieren nur noch die absolut notwendigen Programme
      apt-get update && apt-get install -y curl wget unzip
      # Installiert Google Chrome
      wget -q https://dl.google.com/linux/chrome/deb/pool/main/g/google-chrome-stable/google-chrome-stable_114.0.5735.90-1_amd64.deb
      apt-get install -y ./google-chrome-stable_114.0.5735.90-1_amd64.deb
      # Installiert den passenden ChromeDriver
      wget -q https://chromedriver.storage.googleapis.com/114.0.5735.90/chromedriver_linux64.zip
      unzip chromedriver_linux64.zip
      mv chromedriver /usr/bin/chromedriver
      chmod +x /usr/bin/chromedriver
    # Ein einfacher Start-Befehl, der nur noch den Bot ausführt
    startCommand: python chatbot.py
